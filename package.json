{
  "name": "liquidity-by-murshid",
  "version": "1.0.0",
  "private": true,
  "description": "Liquidity by Murshid AI Subtitle Studio",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"FRONTEND,BACKEND\" --prefix-colors \"yellow,cyan\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "backend\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --app-dir backend --reload --reload-dir backend\\app --host 127.0.0.1 --port 8000",
    "install:frontend": "npm --prefix frontend install",
    "install:backend": "backend\\.venv\\Scripts\\python.exe -m pip install -r backend\\requirements.txt",
    "install:all": "npm run install:frontend && npm run install:backend",
    "build": "npm --prefix frontend run build",
    "check:backend": "backend\\.venv\\Scripts\\python.exe -c \"import fastapi, uvicorn, aiofiles, pydantic, pydantic_settings, faster_whisper; print('Backend dependencies are ready')\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}